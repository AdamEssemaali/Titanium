import "filesystem.ti";
import "system.ti";

func main() : int
{
	// Commento.

	// Tipi di variabile principali.
	var a: int = 32;
	var b: uint = 5;
	var c: float = 5.32;
	var d: ufloat = 5.60;
	var e: double = 6.74832;
	var f: udouble = 93.2837182;
	var g: byte = 120;
	var h: char = 'a';
	var i: string = "Ciao, Mondo!";
	var j: bool = true;
	
	// Modi di scrittura di un valore.
	var hex: int = 0xF8;
	var bin: int = 0b0111011;
	var i: char = 16;

	// Operatori logico-aritmetici.
	// +  Addizione
	// -  Sottrazione
	// /  Divisione
	// *  Moltiplicazione
	// ^  Potenza
	// %  Resto della divisione
	// << Shift a sinistra
	// >> Shift a destra
	// |  Bitwise OR
	// &  Bitwise AND
	// ~  Bitwise NOT

	/*Commento
	a
	più
	linee.*/

	// Strutture dati.
	var intArray: int[] = {5, 3, 2, 6, 1};
	var stringList: string[] = @["Ciao", "Mondo"];
	var dizionario: dict<string, string> = @{"Ciao": "Mondo", "Salve": "Uomo"};
	var tuple: tuple<string, string> = ("Ciao", "Mondo");

	// Struttura di controllo 'if'.
	if (a >= 10 && a <= 31)
	{
		// Do something.
	}
	elif (a > 50)
	{
		// Do something.
	}
	else
	{
		// Do something.
	}

	// Struttura iterativa 'while'.
	while (a > 0)
	{
		// Do something.
	}

	// Struttura iterativa 'do-while'.
	do
	{
		// Do something.
	}
	while (a > 0);

	// Struttura iterativa 'for'.
	for (var i: int = 0; i < 50; i++)
	{
		// Do something.
	}

	// Struttura iterativa 'foreach'.
	foreach (var i: int in intArray)
	{
		// Do something.
	}

	// Operatori di semplificazione.
	a++; // Incremento di una unità (si applica a valori numerici).
	a--; // Decremento di una unità (si applica a valori numerici).
	a += 5;
	a -= 3;
	a /= 5;
	a *= 56;
	a >>= 55;

	i += " stringa concatenata";

	// Built in function.
	println!("Hello, World!");
}

func println(var s: string) : void
{
	println!("frocio > " + s);
}